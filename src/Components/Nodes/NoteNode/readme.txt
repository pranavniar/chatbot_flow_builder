The parent folder of this component will contain another type of node, and when we do make this , we can observe for common code accross different nodes and draw them out into common reauseable sub components.

For instance the header component in text node can have a prop called headerText which would then make it usable across all nodes. 